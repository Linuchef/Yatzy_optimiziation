# Installation

## Stable release

To install Yatzy Strategies, run this command in your terminal:

```sh
uv add python-boilerplate
```

Or if you prefer to use `pip`:

```sh
pip install python-boilerplate
```

## From source

The source files for Yatzy Strategies can be downloaded from the [Github repo](https://github.com/Linuchef/python_boilerplate).

You can either clone the public repository:

```sh
git clone git://github.com/Linuchef/python_boilerplate
```

Or download the [tarball](https://github.com/Linuchef/python_boilerplate/tarball/master):

```sh
curl -OJL https://github.com/Linuchef/python_boilerplate/tarball/master
```

Once you have a copy of the source, you can install it with:

```sh
cd python_boilerplate
uv pip install .
```
